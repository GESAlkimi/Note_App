<div id="lone_note">
	<h1>
		<%= @note.title%>	
	</h1>
	<p>
		<%= @note.body%>
	</p>
	<%= link_to "all Notes", notes_path %>
	<%= link_to "Edit", edit_note_path(@note) %>
	<%= link_to "Delete", note_path(@note), method: :delete, data: {confirm: "Are you sure you would like to delete this note?"} %>
</div>